/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['source.unsplash.com', 'images.unsplash.com', 'placehold.co', 'randomuser.me'],
  },
  reactStrictMode: false,
}

module.exports = nextConfig
